# Supabase (required for CMS + admin auth)
VITE_SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Used by `npm run test:auth` / `npm run test:crud` / `npm run migrate:images` (never commit real values)
ADMIN_EMAIL="admin@almaali.com"
ADMIN_PASSWORD="replace-with-a-strong-password"
